
## "Ignore paths" and add patterns:

src_aws_etl/**               # Ignore entire data pipeline folder
notebooks/**                 # Ignore notebooks
experiments/**               # Ignore experiments  
*.ipynb                      # Ignore all Jupyter notebooks
*.csv                        # Ignore CSV files
*.parquet                    # Ignore Parquet files
data_cache/**                # Ignore data cache
tests/**                     # Ignore tests folder
lambda_assets/**             # Ignore Lambda artifacts


## Settings → Environment Variables

Backend service:
  AWS_ACCESS_KEY_ID = <your-key>
  AWS_SECRET_ACCESS_KEY = <your-secret>
  AWS_DEFAULT_REGION = us-east-1
  
Frontend service:
  (Already set via sevalla.yml: BACKEND_URL=http://backend:8000)

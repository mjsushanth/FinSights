# ============================================================================
# FinRAG Serving Environment Variables
# Instructions: Copy to .env and fill in your values
# ============================================================================

# Backend API Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
BACKEND_RELOAD=true              # Auto-reload on code changes (dev only)

# Frontend Configuration  
FRONTEND_PORT=8501
BACKEND_URL=http://localhost:8000

# Logging
LOG_LEVEL=INFO                   # DEBUG, INFO, WARNING, ERROR
LOG_FORMAT=json                  # json or text

# Caching (Optional)
ENABLE_CACHE=true
CACHE_TTL_SECONDS=300            # 5 minutes

# AWS Credentials (Backend uses ML environment's AWS config)
# These should already be configured in your ML environment
# AWS_PROFILE=default
# AWS_REGION=us-east-1

# Model Root Path (Auto-detected, but can override)
# MODEL_ROOT=/path/to/ModelPipeline/finrag_ml_tg1

# ============================================================================
# NOTES
# ============================================================================
# 
# SETUP:
#   1. Copy this file: cp .env.example .env
#   2. Edit .env with your values
#   3. Both backend and frontend will load these
#
# SECURITY:
#   - Never commit .env to git (already in .gitignore)
#   - Use separate .env files for dev/staging/prod
#   - Rotate credentials regularly
#
# ============================================================================